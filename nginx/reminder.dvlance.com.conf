# Redirección de HTTP a HTTPS
server {
    listen 80;
    server_name reminder.dvlance.com;
    location /.well-known/acme-challenge/ {
        alias /app/public/.well-known/acme-challenge/;
        try_files $uri =404;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}
